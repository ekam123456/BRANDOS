import Link from "next/link";
import { EmptyState, ProductHeader, ProductShell } from "@/components/ProductShell";
export default function BrainPage() { return <ProductShell><ProductHeader eyebrow="Business Brain" title="What BRANDOS knows about your business." description="This is the understanding layer: facts, decisions, assumptions, and open questions kept distinct." /><div className="brain-tabs" role="tablist">{["Business", "Customers", "Products", "Market", "Goals"].map((item) => <button key={item} type="button">{item}</button>)}</div><EmptyState title="Your Business Brain is just getting started." action={<Link className="button" href="/onboarding">Add business context</Link>}>There are no verified business records yet. Start with the guided setup so BRANDOS can learn without guessing.</EmptyState></ProductShell>; }
