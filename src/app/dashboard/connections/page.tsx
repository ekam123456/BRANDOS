import Link from "next/link";
import { EmptyState, ProductHeader, ProductShell, StatusBadge } from "@/components/ProductShell";
export default function ConnectionsPage() { return <ProductShell><ProductHeader eyebrow="Connections" title="Connect your business, carefully." description="A connection is shown as active only after it is actually verified. Each source should explain what BRANDOS can learn from it." /><section className="connection-panel"><div><span className="kicker">Available in the foundation</span><h2>No integrations are implemented yet.</h2><p>You can continue without connections. Future sources will show their status, scope, and last successful sync here.</p></div><StatusBadge>Not connected</StatusBadge></section><EmptyState title="No tools connected yet." action={<Link className="button secondary" href="/onboarding">Review setup</Link>}>BRANDOS can use real business data once a supported source is connected and verified.</EmptyState></ProductShell>; }
