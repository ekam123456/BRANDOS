# BRANDOS AI architecture

AI behavior is not implemented in this milestone. The current foundation deliberately stops at authenticated organization context, business setup, and authorization boundaries.

Future intelligence must read tenant-scoped PostgreSQL records, explain evidence and uncertainty, and write recommendations or tasks only through authorized domain services. No model may receive another organization’s records, and no public route should imply that intelligence is already operating.
