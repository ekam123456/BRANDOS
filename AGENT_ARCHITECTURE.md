# BRANDOS agent architecture

Agents are planned, not implemented, in the current Stage 2 foundation.

When introduced, each agent will have an explicit capability set, tenant context, authorization check, approval policy, execution record, and audit event. Agent tools will be server-side only and will never use a client-supplied organization ID as authority.
